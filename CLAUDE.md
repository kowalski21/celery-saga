# CLAUDE.md

## Environment

Use the conda environment `hacks` for this project. Activate it before running any commands:

```bash
conda activate hacks
```

Install any pip packages you need into this environment (do not create a new venv):

```bash
conda activate hacks && pip install <package>
```
# Git
Do not add Co Authored by Git to commit messages